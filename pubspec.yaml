name: mewing_frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^0.8.6+4
  path_provider: ^2.0.11
  path: ^1.8.1
  http: ^1.2.0 # Menghapus duplikat

  # Untuk validasi formulir data input
  form_field_validator: ^1.1.0

  # Untuk state management opsional
  provider: ^6.0.4

  # HTTP request jika terhubung backend
  # Sudah ada dalam bagian sebelumnya, tidak perlu diulang

  # Untuk animasi sederhana
  animated_text_kit: ^4.2.2

  # Icon tambahan dari material design
  flutter_feather_icons: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# flutter:
#   uses-material-design: true
#   fonts:
#     - family: Poppins
#       fonts:
#         - asset: fonts/Poppins-Regular.ttf
#         - asset: fonts/Poppins-Bold.ttf
#           weight: 700
